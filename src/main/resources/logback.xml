<?xml version="1.0" encoding="UTF-8"?>

<configuration>

    <include resource="logback-base.xml" />

    <!--
        This is a command line tool, so log to command line. 
     -->
    <root level="INFO">
        <appender-ref ref="ROLLING_FILE" />
    </root>
</configuration>
